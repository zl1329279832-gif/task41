<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fc.v2.mapper.custom.NoticeDao">

  <!-- 根据角色ID列表查询关联的用户ID（快照） -->
  <select id="selectUserIdsByRoleIds" resultType="String">
    SELECT DISTINCT ru.sys_user_id
    FROM t_sys_role_user ru
    WHERE ru.sys_role_id IN
    <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
      #{roleId}
    </foreach>
  </select>

  <!-- 根据部门ID列表查询关联的用户ID（快照） -->
  <select id="selectUserIdsByDeptIds" resultType="String">
    SELECT DISTINCT u.id
    FROM t_sys_user u
    WHERE u.dep_id IN
    <foreach collection="deptIds" item="deptId" open="(" separator="," close=")">
      #{deptId}
    </foreach>
  </select>

  <!-- 统计用户未读公告数量（排除已撤回的公告 status=1） -->
  <select id="countUnreadByUserId" resultType="int">
    SELECT COUNT(*)
    FROM t_sys_notice_user nu
    INNER JOIN t_sys_notice n ON nu.notice_id = n.id
    WHERE nu.user_id = #{userId}
      AND nu.state = 0
      AND n.status != 1
  </select>

  <!-- 批量插入 notice_user 记录 -->
  <insert id="batchInsertNoticeUser">
    INSERT INTO t_sys_notice_user (id, notice_id, user_id, state)
    VALUES
    <foreach collection="list" item="item" separator=",">
      (#{item.id}, #{item.noticeId}, #{item.userId}, #{item.state})
    </foreach>
  </insert>

  <!-- 用户公告分页列表：JOIN t_sys_notice + t_sys_notice_user，排除已撤回 -->
  <select id="selectUserNoticePage" resultType="com.fc.v2.model.auto.SysNotice">
    SELECT n.id, n.title, n.content, n.type, n.create_id AS createId,
           n.create_username AS createUsername, n.create_time AS createTime,
           n.status, n.scope_type AS scopeType,
           nu.state, nu.read_time AS readTime
    FROM t_sys_notice n
    INNER JOIN t_sys_notice_user nu ON n.id = nu.notice_id
    WHERE nu.user_id = #{userId}
      AND n.status != 1
    <if test="title != null and title != ''">
      AND n.title LIKE CONCAT('%', #{title}, '%')
    </if>
    ORDER BY n.create_time DESC
  </select>

  <!-- 用户公告详情（单条 JOIN 查询，含 state 和 read_time） -->
  <select id="selectUserNoticeDetail" resultType="com.fc.v2.model.auto.SysNotice">
    SELECT n.id, n.title, n.content, n.type, n.create_id AS createId,
           n.create_username AS createUsername, n.create_time AS createTime,
           n.status, n.scope_type AS scopeType, n.scope_ids AS scopeIds,
           nu.state, nu.read_time AS readTime
    FROM t_sys_notice n
    INNER JOIN t_sys_notice_user nu ON n.id = nu.notice_id
    WHERE n.id = #{noticeId}
      AND nu.user_id = #{userId}
      AND n.status != 1
  </select>

  <!-- 查询公告的已读/未读统计 -->
  <select id="selectReadStatByNoticeId" resultType="com.fc.v2.model.custom.NoticeReadStatVO">
    SELECT
      COUNT(*) AS totalCount,
      IFNULL(SUM(CASE WHEN state = 1 THEN 1 ELSE 0 END), 0) AS readCount,
      IFNULL(SUM(CASE WHEN state = 0 THEN 1 ELSE 0 END), 0) AS unreadCount
    FROM t_sys_notice_user
    WHERE notice_id = #{noticeId}
  </select>

</mapper>
